//Prompt the user to enter their full name.
//Generate the username for them based on the input 
//start username with @, followed by there username and ending with fullname length.

let fullName = prompt("Enter ypur Full name without spaces");

console.log(fullName);

let name = `Ankit got ${100} marks in exam`;

console.log(name);

let userName = "@" + fullName + fullName.length;

console.log(userName);
